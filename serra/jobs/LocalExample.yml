## Fails, not updated with input blocks

debug: True

read_sales:
  class_name: LocalReader
  config:
    file_path: "./examples/sales.csv"

read_ratings:
  class_name: LocalReader
  config:
    file_path: "./examples/ratings.csv"
  tests:
    - duplicates

step_join:
  class_name: JoinTransformer
  config:
    join_type: 'inner'
    join_on:
      read_sales: id
      read_ratings: id
  tests:
    - nulls
