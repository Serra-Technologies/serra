step_read:
  class_name: DatabricksReader
  config:
    database: 'demo'
    table: 'test_table'

step_write:
  class_name: DatabricksWriter
  config:
    input_block: step_read
    database: 'demo'
    table: 'db_test'
    format: 'delta'
    mode: 'overwrite'
